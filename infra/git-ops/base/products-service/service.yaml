apiVersion: v1
kind: Service
metadata:
  name: products-service
  labels:
    app.kubernetes.io/name: products-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: products-service
  ports:
    - name: http
      port: 80
      targetPort: http

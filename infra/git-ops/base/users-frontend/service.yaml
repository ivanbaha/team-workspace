apiVersion: v1
kind: Service
metadata:
  name: users-frontend
  labels:
    app.kubernetes.io/name: users-frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: users-frontend
  ports:
    - name: http
      port: 80
      targetPort: http

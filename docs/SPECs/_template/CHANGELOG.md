# Changelog

All notable changes to this spec are recorded here.

The format follows [Keep a Changelog](https://keepachangelog.com/). Versioning starts at
1.0 (the initial spec); only subsequent changes are logged.

<!--
  Amendments go into THIS spec by default — including defects found after the feature is
  live. Edit the affected criteria in place, keep their numbers, annotate inline, bump the
  version in README.md, and add an entry here.

  A code-only fix that changes nothing documented needs no version bump, but still gets a
  line under "Fixed (code-only, no spec change needed)" so the record is complete.

  See docs/sdlc/02-specification.md § Amendments.
-->

---

## [1.1] — <YYYY-MM-DD>

**Source:** [<CODE>](<url>) — <what prompted the change: a defect, a follow-up ticket, a
blocker found during implementation>

### Changed

- <requirement, design section or task that changed, and what it says now>

### Added

- <new requirement, task or testing scope>

### Fixed (code-only, no spec change needed)

- <bug fixed during implementation, recorded for traceability>

### Impact

- Updated: `<file>` (<what changed in it>)
- Created: `<file>`

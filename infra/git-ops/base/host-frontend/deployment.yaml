apiVersion: apps/v1
kind: Deployment
metadata:
  name: host-frontend
  labels:
    app.kubernetes.io/name: host-frontend
    app.kubernetes.io/part-of: team-workspace
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: host-frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: host-frontend
        app.kubernetes.io/part-of: team-workspace
    spec:
      containers:
        # The container name is load-bearing: DEPLOYMENT_NAME below must equal it,
        # or this service appears as an orphaned root in every trace it takes part
        # in — while each individual log line still looks perfectly correct.
        - name: host-frontend
          image: registry.example.com/team-workspace/host-frontend:0.0.0-base
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: DEPLOYMENT_NAME
              value: host-frontend
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          # Per-environment configuration. The ConfigMap is generated by each
          # overlay from overlays/<env>/config/host-frontend.env, so a value that
          # differs between environments lives in exactly one place.
          envFrom:
            - configMapRef:
                name: host-frontend-env
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 128Mi
